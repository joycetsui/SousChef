<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.workingtitle.makeit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalClass"
        android:allowBackup="true"
        android:icon="@mipmap/souscheficon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".controllers.SearchOptions">
        </activity>

        <activity
            android:name=".controllers.SearchResults"
            android:parentActivityName=".controllers.MainActivity" >
        </activity>

        <activity
            android:name=".controllers.DisplayRecipe"
            android:parentActivityName=".controllers.MainActivity" >
        </activity>

        <activity
            android:name=".controllers.UserSettingsActivity"></activity>
    </application>

</manifest>